@model Publisher

@if (TempData["InfoDeleted"] is not null)
{
    <h3 class="text-center text-danger">@TempData["InfoDeleted"]</h3>
}

<table class="table table-bordered">
    <tr>
        <th class="text-center">Name</th>
        <th class="text-center">Address</th>
        <th class="text-center">Phone</th>
        <th class="text-center">Foundation Date</th>
        <th class="text-lg-center">Please Select The Action</th>
    </tr>
    <tr>
        <td>@Model.Name</td>
        <td>@Model.Address</td>
        <td>@Model.Phone</td>
        <td>@Model.FoundationDate</td>
        <td class="text-center">
            <form method="post" asp-action="Delete2" asp-controller="Publisher">
                <input type="hidden" asp-for="ID" />
                <button type="submit" class="btn btn-outline-primary">Delete Publisher</button>
            </form>
        </td>
    </tr>
</table>

